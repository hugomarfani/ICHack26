from __future__ import annotations

from typing import Dict


def apply_rules(text: str) -> Dict[str, Dict[str, object]]:
    """Rules intentionally disabled: semantic-only selection; JRCalc handled elsewhere."""
    return {}
